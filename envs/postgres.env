# PostgreSQL Database Configuration
# PostgreSQL データベース設定
# configuration: 設定

# PostgreSQL Database Host
# host: ホスト、データベースサーバーのアドレス
POSTGRES_HOST=postgres

# PostgreSQL Database Port
# port: ポート、接続用のポート番号
POSTGRES_PORT=5432

# PostgreSQL Database Name
# database: データベース、name: 名前
POSTGRES_DB=sift_app_db

# PostgreSQL Database User
# user: ユーザー、データベースアクセス用のユーザー名
POSTGRES_USER=sift_user

# PostgreSQL Database Password
# password: パスワード、認証用のパスワード
POSTGRES_PASSWORD=sift_password_2024

# PostgreSQL Connection Settings for Application
# connection: 接続、settings: 設定、application: アプリケーション
DB_HOST=postgres
DB_PORT=5432
DB_NAME=sift_app_db
DB_USER=sift_user
DB_PASSWORD=sift_password_2024

# SSL Mode Configuration
# ssl: セキュリティ層、mode: モード、configuration: 設定
DB_SSL_MODE=disable

# PostgreSQL Memory and Performance Settings
# memory: メモリ、performance: パフォーマンス、settings: 設定
POSTGRES_SHARED_BUFFERS=256MB
POSTGRES_EFFECTIVE_CACHE_SIZE=1GB
POSTGRES_WORK_MEM=4MB
POSTGRES_MAINTENANCE_WORK_MEM=64MB

# PostgreSQL Logging Configuration
# logging: ログ記録、configuration: 設定
POSTGRES_LOG_STATEMENT=all
POSTGRES_LOG_MIN_DURATION_STATEMENT=1000

# PostgreSQL Authentication Configuration
# authentication: 認証、configuration: 設定
POSTGRES_AUTH_METHOD=md5 